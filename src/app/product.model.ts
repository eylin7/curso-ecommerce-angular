 export interface Product { // la interface de typescript nos permite 
    name: string;            // tipar con la palabra clave interface que 
    price: number;           // que es una forma de ver cada atritubo de cada 
    image: string;            // objeto 
    category?: string;
}